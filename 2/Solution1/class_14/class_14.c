//Derive dTypes(유도 자료형)

//Array(배열): store multiple values of same type
int scores[5] = { 90, 85, 77, 92, 88 }; //declare 5 int types

//Pointer(포인터): stores address of another variable
int* p = &x; //p holds(save) address of x

//Function(함수): defined by return type + parameters
int add(int a, int b) { return a + b; }
